<% participant_count = 0 %>

<% conversation.participants.each do |participant, index| %>
  <% if participant != current_user %>
    <% participant_count += 1 %>

    <% if (participant_count != 1) %>
      ,&nbsp;
    <% end %>

    <%=participant.first_name + " " + participant.last_name %>
  <% end %>
<% end %>
